%POLAR COORDINATES
%The print template for A4 paper (portrait)
%Author: Zoran Nikolic
\documentclass[12pt]{article}
\usepackage[margin=0.5in,paper=a4paper]{geometry} %Shrinking margins to 0.5in
\usepackage[x11names]{xcolor}                     %Additional colors
\usepackage{tikz}
\usetikzlibrary{arrows.meta, calc, quotes}

\usepackage{verbatim}
\usepackage[active,tightpage]{preview}
\PreviewEnvironment{tikzpicture}
\setlength\PreviewBorder{5pt}%

\usepackage{euler}
\usepackage{pgfplots}
\usepgfplotslibrary{polar}

\begin{document}
\thispagestyle{empty}

\begin{center}
\begin{tikzpicture}
\begin{scope}
\clip (-12,-12) rectangle (3.5,12);
%Circles
\foreach \r in {1, 2,...,10} \draw[rotate=90,SteelBlue3, thick] (0,0) circle (\r);
\foreach \r in {0.5, 1.5,...,10} \draw[rotate=90,Azure4, thin] (0,0) circle (\r);
%1° Rays
\foreach \a in {0, 1,...,359} \draw[rotate=90,Azure4] (\a:10.7) -- (\a:11);
%5° Rays
\foreach \a in {0, 5,...,359} \draw[rotate=90,Azure4] (\a:10.5) -- (\a:11);
%15° Rays
\foreach \a in {0, 15,...,359} \draw[rotate=90,thick,Azure4] (\a:1) -- (\a:11);
%30° Rays
\foreach \a in {0, 30,...,359} \draw[rotate=90,thick,Azure4] (0, 0) -- (\a:11);
%Main rays
\foreach \a in {0, 90,...,359} \draw[rotate=90,very thick] (0, 0) -- (\a:11);
%Angle labels
\foreach \a in {0, -15,...,-180} \draw[rotate=90,yscale=-1,xscale=1] (\a: 11.5) node {$\a^\circ$};
%\foreach \a in {15, 30,...,180} \draw[rotate=90,yscale=-1,xscale=1] (\a: 11.5) node {$\a^\circ$};
%Central point
\draw[fill=red] (0,0) circle(0.7mm);
% Now the additional plot:
\begin{polaraxis}[rotate=90,hide axis,anchor=origin,disabledatascaling,at={(0pt,0pt)},x=1cm,y=1cm]
\addplot+[mark=none,domain=0:720,samples=600,ultra thick] {10+(5.73*(cos(x)))+(8.19*(sin(x)))};
%\addplot+[mark=none,domain=0:720,samples=600,ultra thick,yshift=-2.125cm,xshift=2.125cm] {(10+(3.42*(cos(x)))-(9.39*(sin(x))))*0.6};
\end{polaraxis}
\end{scope}

\begin{scope}[xshift=8.5cm, yshift=0]
\clip (-3.5,-12) rectangle (12,12);
%Circles
\foreach \r in {1, 2,...,10} \draw[rotate=90,SteelBlue3, thick] (0,0) circle (\r);
\foreach \r in {0.5, 1.5,...,10} \draw[rotate=90,Azure4, thin] (0,0) circle (\r);
%1° Rays
\foreach \a in {0, 1,...,359} \draw[rotate=90,Azure4] (\a:10.7) -- (\a:11);
%5° Rays
\foreach \a in {0, 5,...,359} \draw[rotate=90,Azure4] (\a:10.5) -- (\a:11);
%15° Rays
\foreach \a in {0, 15,...,359} \draw[rotate=90,thick,Azure4] (\a:1) -- (\a:11);
%30° Rays
\foreach \a in {0, 30,...,359} \draw[rotate=90,thick,Azure4] (0, 0) -- (\a:11);
%Main rays
\foreach \a in {0, 90,...,359} \draw[rotate=90,very thick] (0, 0) -- (\a:11);
%Angle labels
%\foreach \a in {0, -15,...,-179} \draw[rotate=90,yscale=-1,xscale=1] (\a: 11.5) node {$\a^\circ$};
\foreach \a in {0,15,...,180} \draw[rotate=90,yscale=-1,xscale=1] (\a: 11.5) node {$\a^\circ$};
%Central point
\draw[fill=red] (0,0) circle(0.7mm);
% Now the additional plot:
\begin{polaraxis}[rotate=90,hide axis,anchor=origin,disabledatascaling,at={(0pt,0pt)},x=1cm,y=1cm]
\addplot+[mark=none,domain=0:720,samples=600,ultra thick,color=red] {10+(5.73*(cos(x)))-(8.19*(sin(x)))};
%\addplot+[mark=none,domain=0:720,samples=600,ultra thick,yshift=-2.125cm,xshift=2.125cm] {(10+(3.42*(cos(x)))-(9.39*(sin(x))))*0.6};
\end{polaraxis}
\end{scope}

\begin{scope}[
  %scale=0.5,
  auto = right,
  measure/.style = {
    ultra thick,
    {Straight Barb[]}-%
    {Straight Barb[]},
  }
]
\coordinate[]     (b) at (0,0);
\coordinate[]    (c) at (8.5,0);
\draw[measure]  ($(b)!30mm!-90:(c)$) to [sloped,"$17cm$"] ($(c)!30mm! 90:(b)$);
    \end{scope}
\end{tikzpicture}
\end{center}

\end{document}
